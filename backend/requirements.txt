# RetinaSight AI — backend base dependencies (runnable without heavy ML libs).
# Heavy/optional ML deps live in requirements-ml.txt.

fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0
email-validator>=2.1,<3.0

SQLAlchemy>=2.0.30,<2.1
alembic>=1.13,<2.0
# Postgres driver for production parity (SQLite is stdlib for dev).
psycopg[binary]>=3.1,<4.0

PyJWT>=2.8,<3.0
argon2-cffi>=23.1,<24.0
python-multipart>=0.0.9,<1.0

# Object storage (S3-compatible).
boto3>=1.34,<2.0

# Light image + numeric handling used by the honest ML stub / quality gate.
numpy>=1.26,<3.0
Pillow>=10.3,<12.0

# HTTP client (also used by tests).
httpx>=0.27,<1.0

# --- test/dev tooling ---
pytest>=8.2,<9.0
pytest-asyncio>=0.23,<1.0
